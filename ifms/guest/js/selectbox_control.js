/*** 사용신청조회 ***/
	$(function () {
		$(".sorted_days").selectbox();
	});

/*** 신청인 ***/
	$(function () {
		$(".sorted_p").selectbox();
	});

/*** 상태 ***/
	$(function () {
		$(".sorted_progress").selectbox();
	});

/*** 이용자별 이력조회 ***/
	$(function () {
		$(".sorted_months_user").selectbox();
	});

/*** 공동시설별 이력조회 ***/
	$(function () {
		$(".sorted_months_group").selectbox();
	});

/*** 공동시설등록 ***/
	$(function () {
		$(".sorted_fac").selectbox();
	});

/*** 시간 (관리자 페이지) ***/
	$(function () {
		$(".time_hour").selectbox();
	});

/*** 분 (관리자 페이지) ***/
	$(function () {
		$(".time_minute").selectbox();
	});

/*** 시간 (입주자 페이지) ***/
	$(function () {
		$(".time_hour2").selectbox();
	});

/*** 분 (입주자 페이지) ***/
	$(function () {
		$(".time_minute2").selectbox();
	});

/*** 담당자 ***/
	$(function () {
		$(".sorted_officer").selectbox();
	});

/*** 사용자 권한 ***/
	$(function () {
		$(".sorted_perm").selectbox();
	});

/*** 비품유형 ***/
	$(function () {
		$(".sorted_goods").selectbox();
	});

/*** SMS수정 - 서비스명 ***/
	$(function () {
		$(".sorted_service").selectbox();
	});

/*** 민원유형 ***/
	$(function () {
		$(".sorted_case").selectbox();
	});

/*** 시설유형 ***/
	$(function () {
		$(".sorted_room").selectbox();
	});